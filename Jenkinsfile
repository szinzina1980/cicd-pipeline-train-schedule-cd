pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
                echo 'Continuation Integration build'
            }
        }
    }
}
